# Bad YAML - Overly long lines
apiVersion: v1
kind: ConfigMap
metadata:
  name: long-lines-config
  namespace: default
  labels:
    app: example
    version: "1.0"
    description: "This is a very long description that exceeds the recommended line length limit and should trigger a line length warning in the cosmetics checker"
  annotations:
    description: "This is another very long description that exceeds the recommended line length limit and should trigger a line length warning in the cosmetics checker"
data:
  database:
    host: localhost
    port: 5432
    connection_string: "postgresql://username:password@localhost:5432/database?sslmode=require&connect_timeout=30&application_name=myapp&search_path=public,schema1,schema2"
    credentials:
      username: admin
      password: secret
      description: "This is a very long description for the database credentials that exceeds the recommended line length limit"
  redis:
    host: localhost
    port: 6379
    password: redis-secret
    connection_string: "redis://username:password@localhost:6379/0?timeout=30&retry_on_timeout=true&socket_keepalive=true&socket_keepalive_options=1,3,5"
  features:
    - authentication
    - authorization
    - logging
    - monitoring
    - very-long-feature-name-that-exceeds-recommended-line-length
