# Bad YAML - Mixed quote usage
apiVersion: v1
kind: ConfigMap
metadata:
  name: "mixed-quotes-config"
  namespace: 'default'
  labels:
    app: "example"
    version: '1.0'
    description: "This YAML has mixed quote usage"
  annotations:
    description: 'This YAML has mixed quote usage'
data:
  database:
    host: "localhost"
    port: '5432'
    credentials:
      username: "admin"
      password: 'secret'
  redis:
    host: "localhost"
    port: '6379'
    password: "redis-secret"
  features:
    - "authentication"
    - 'authorization'
    - "logging"
    - 'monitoring'
